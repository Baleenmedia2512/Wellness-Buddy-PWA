/**
 * @deprecated Import from `shared/services/userIdentity` instead.
 * Kept as a thin re-export proxy to avoid breaking forgotten
 * consumers; will be removed once all imports are migrated.
 */
export { getUserId, clearUserIdCache } from '../../../shared/services/getUserId.js';
